# Plan Mode

Plan mode is useful when tasks need step-by-step tracking.

## Typical Use Cases

- Multi-file migrations
- Feature implementation with dependencies
- Work requiring checkpoints

Switch inside the session with `/plan` and return with `/build`.
