# Tools and Approval

ByteMind can read files, search text, write patches, and run shell commands.

## Safe Default

Set `approval_policy` to `on-request` so sensitive actions stay user-approved.

## Typical Workflow

1. Ask the agent to inspect relevant files.
2. Review proposed edits.
3. Approve commands or file changes when ready.
