# Provider Setup

ByteMind supports:

- OpenAI-compatible providers
- Anthropic

## Setup Steps

1. Set `provider.type`.
2. Set `base_url`, `model`, and `api_key`.
3. Run `bytemind chat` and verify requests succeed.

For complete fields, see [Config Reference](/en/reference/config-reference).
