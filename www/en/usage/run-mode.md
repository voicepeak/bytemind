# Run Mode

Use run mode for one-shot tasks.

```bash
bytemind run -prompt "update README installation section"
```

## When to Use

- CI-like documentation tasks
- Single refactor requests
- Non-interactive scripts
