# Example: Fix a Bug

Prompt example:

```text
Investigate why login API returns 500 in integration tests, apply a minimal fix, and add a regression test.
```

Expected outcome:

- Root cause identified
- Minimal code change
- Test coverage for the bug path
