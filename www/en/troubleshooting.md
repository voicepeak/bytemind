# Troubleshooting

## Command Not Found

Ensure your install directory is in `PATH`.

## Provider Authentication Failed

Verify `api_key`, `base_url`, and `model` in `.bytemind/config.json`.

## Slow or Incomplete Execution

Increase `max_iterations` for larger tasks.
