# 配置参考

`config.json` 常用字段：

- `provider`：模型 provider 配置
- `approval_policy`：高风险操作审批策略
- `max_iterations`：单任务最大工具轮次
- `stream`：流式输出开关
- `update_check.enabled`：更新检查开关

完整示例见 [`config.example.json`](https://github.com/1024XEngineer/bytemind/blob/main/config.example.json)。
